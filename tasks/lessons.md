# Lessons

## 2026-02-27

### 用户纠偏：不要停在“发现上层仓库脏改动”这一步
- 触发场景：目录位于上层大型 Git 仓库内，`git status` 输出大量无关文件。
- 本次修正规则：
  - 先在目标项目目录初始化独立 `.git` 仓库，再继续提交/推送。
  - 仅在确实会影响目标目录改动时再向用户中断确认。
  - 需要发布代码时，优先保证“目标项目可独立版本化”再做其余收尾工作。

### 用户纠偏：公式复制不能带异常换行，且要参考现有成熟实现
- 触发场景：直接读取 KaTeX annotation 文本时，复制结果出现多余换行，影响 LaTeX 可用性。
- 本次修正规则：
  - 公式复制必须先做 LaTeX 源码归一化（去除硬换行、合并多余空白）。
  - 遇到已有参考项目时，先迁移其能力模型（识别/格式/复制回退/提示），再做页面适配。
  - 复杂交互优先模块化（独立服务文件），主内容脚本仅做装配与配置同步。

### 用户纠偏：按阶段推进迁移，先完成 timeline 再扩展其它能力
- 触发场景：需求包含多个迁移项时，用户明确要求先做 timeline。
- 本次修正规则：
  - 接到阶段化指令后，先聚焦当前阶段（timeline）并闭环验证，再进入下一批功能。
  - 每一阶段都要输出可运行产物（代码+打包）并同步文档状态。
  - 避免在同一迭代混入过多并行功能，保持改动职责边界清晰。

### 用户纠偏：用户限定“只迁移这几个”时，严格控制本轮范围
- 触发场景：用户点名只迁移 `sidebarAutoHide` / `folderSpacing` / `markdownPatcher` / `snowEffect`。
- 本次修正规则：
  - 本轮实现仅覆盖用户点名模块，不夹带其它迁移项。
  - 若需要公共装配改动（storage/面板），只做与这几项直接相关的最小改动。
  - 回报结果时明确“已完成模块”和“未触碰模块”，避免范围歧义。

### 用户纠偏：迁移功能后还要统一 UI，并把导出能力作为主路径
- 触发场景：功能已迁移但面板信息密度高、导出与增强配置混在一起，用户要求与 Gemini 插件保持统一体验。
- 本次修正规则：
  - 功能迁移完成后，必须补做信息架构收敛（导出主路径与配置分区分离）。
  - 视觉迁移不只改颜色，需同步改布局层次（tab/分组）和状态持久化。
  - 新增功能进入面板时，优先放入“工作区”分区，避免挤占导出首屏路径。

### 用户纠偏：设置入口应放在扩展图标 popup，页面内不保留悬浮标
- 触发场景：页面悬浮按钮占位且干扰阅读，用户要求将配置入口迁移到浏览器顶部扩展图标。
- 本次修正规则：
  - 设置类入口默认优先放 `action popup`，页面内仅保留必要的内容辅助 UI。
  - 对于已迁移到 popup 的设置，content script 必须支持消息补丁并实时生效，避免“改了要刷新”。
  - 时间轴等高频交互功能要优先优化响应路径（滚动节流、二分命中、搜索防抖、滚轮隔离），再做视觉调整。

### 用户纠偏：时间线要“可拖、可跳、可快速加载”，导出设置需贴近时间线
- 触发场景：时间线交互滞后（加载慢、圆点跳转失效、拖拽区域太窄）且导出设置入口位置不符合操作路径。
- 本次修正规则：
  - 时间线默认位置放右侧；除圆点附近外，整条轨道都应可拖拽并反馈手型光标。
  - 时间线刷新必须走轻量数据通路（快速 turns 采集 + 独立刷新节奏），不能依赖导出全量解析完成。
  - 导出参数若主要服务时间线跳转上下文，应优先放在时间线附近（预览面板下方）；popup 保持全局设置收敛。

### 用户纠偏：公式交互要“背景即按钮”，独立公式不应高亮整行空白
- 触发场景：公式悬停时复制按钮干扰阅读，且块级公式背景覆盖了左右留白、上下边缘包裹不完整。
- 本次修正规则：
  - 公式复制默认走“点击公式节点即复制”，不额外显示按钮。
  - 块级公式只给公式本体节点加高亮，不给整行容器加背景。
  - 高亮样式必须通过 padding + 负 margin 修正行高边缘，避免上下露边。

### 用户纠偏：发布前需要统一产品命名并完善 README
- 触发场景：用户要求同步 GitHub 时同时更换产品名，并将 README 升级为正式项目文档。
- 本次修正规则：
  - 发布前先做命名一致性检查（manifest/popup/面板文案/日志前缀/打包产物名）。
  - README 不能只做增量补丁，应按“定位-能力-使用-构建-结构”整体重写。
  - 推送前需完成一次完整校验（语法 + 打包）再提交远端。

### 用户纠偏：时间线交互应“快反馈、单面板、均匀分布”
- 触发场景：用户反馈圆点点击跳转慢、滚动高亮滞后、圆点聚集在上方，以及预览/导出分散在多个浮层导致点击体验差。
- 本次修正规则：
  - 圆点点击必须先激活再滚动，动画时长以“快速可感知”优先。
  - 圆点布局优先使用比例均匀分布，避免固定像素间距造成密度失衡。
  - 预览与导出优先合并到同一个工作面板，减少浮层切换与指针移动距离。

### 用户纠偏：popup 中的设置必须“可见即可用”，伪功能要及时移除
- 触发场景：用户指出 popup 内多项开关没有实际行为（如拖拽时间线、滚动模式无差异、上下文同步不清晰）。
- 本次修正规则：
  - popup 每新增一个配置项，必须同步落地存储键、content 实现与实时生效链路。
  - 无实际价值或未实现的入口（例如可拖拽时间线）不保留在 UI 中，避免误导。
  - 与时间线强相关的预览/导出交互优先靠近时间线本体，用 hover-intent 与智能翻侧提升可达性。

### 用户纠偏：ChatGPT 改版频繁，侧栏与时间线可见性要做结构自愈
- 触发场景：用户反馈“文件夹不出现、时间线不显示”，说明 DOM 选择器和响应式隐藏规则对新版布局不稳。
- 本次修正规则：
  - 侧栏挂载优先基于真实会话链接（`/c/`）反推容器，并用 MutationObserver 做自动重挂载。
  - 时间线样式不要在中等视口直接隐藏，仅在必要元素（tooltip/menu）降级隐藏。
  - 每次改交互后都要进行“窄窗口 + 新版侧栏”可见性回归检查。

### 用户纠偏：时间线交互要分层触发，性能优化要先控刷新链路
- 触发场景：用户反馈“时间线卡顿”，并要求“先靠近时间线出现小图标，再靠近小图标才展开预览”，同时要求原生会话标题可拖拽到文件夹。
- 本次修正规则：
  - 时间线预览必须采用两段式 hover-intent：`bar -> launcher -> preview`，避免直接弹大面板干扰阅读。
  - 性能优化优先从刷新入口做减法：MutationObserver 先做相关性过滤与聚合，再触发 timeline/meta/heavy 刷新。
  - 文件夹归档要支持“原生会话条目直接拖拽”而非仅扩展内按钮操作，并提供拖拽源/目标双向视觉反馈。

### 用户纠偏：popup 设置必须所见即所得，默认值要通过迁移真正落地
- 触发场景：用户要求“时间轴默认开启”且“popup 显示设置点击后立即在页面生效”。
- 本次修正规则：
  - 默认值变更不能只改常量，必须加一次性迁移键把旧用户存量设置同步到新默认。
  - popup 的显示设置优先使用实时事件（checkbox/range 的 `input`），并立即下发到当前页面。
  - 设置反馈要区分“已实时应用”和“仅保存未连接页面”，避免误导用户认为已经生效。

### 用户纠偏：popup 功能要做“端到端审计”，不是只看开关存在
- 触发场景：用户要求再次验证 popup 功能是否都实现，说明此前存在“UI 有项但行为覆盖不完整”的风险。
- 本次修正规则：
  - 每次新增/调整 popup 项后，必须核对 `popup 控件 -> storage key -> content patch -> feature 行为` 四段链路。
  - 不只检查“有代码入口”，还要检查是否覆盖主界面与侧栏等多处 UI（例如 folder spacing）。
  - 审计后要输出缺口清单并在同轮补齐，不把验证和修复拆开。

### 用户纠偏：宿主页面样式与扩展 UI 冲突要做标签与可见性兜底
- 触发场景：用户反馈时间轴不可见，根因来自宿主样式对通用标签（如 `aside`）的潜在干扰。
- 本次修正规则：
  - 扩展浮层容器优先用 `div`，避免使用易被站点全局样式命中的语义标签。
  - 对关键 UI（时间轴）在运行时增加可见性兜底（`display/visibility/opacity`），确保启用即可见。
  - 设置同步链路除 runtime message 外，增加 storage 监听兜底，提升“即改即见”稳定性。

## 2026-02-28

### 用户纠偏：侧栏数据同步不能每次都改时间戳，否则会造成下拉抖动与高占用
- 触发场景：用户点击“当前会话分组”下拉时，选项反复刷新，时间轴与侧栏都出现卡顿。
- 本次修正规则：
  - 侧栏会话同步必须做增量更新：数据未变化时禁止 `render + persist`。
  - 不要在每轮 DOM 采集中给所有会话写 `updatedAt=Date.now()`；仅在新会话或会话切换时更新。
  - 对 `select` 交互增加短时锁（focus/pointerdown/key），交互窗口内冻结 options/value 重写。

### 用户纠偏：时间轴可见性要有“自恢复”机制，不能只依赖初始化一次
- 触发场景：用户反馈“时间轴又看不到了”，说明宿主页面重排后会让时间轴节点丢失或被样式覆盖。
- 本次修正规则：
  - 时间轴启用态必须有可见性守护：`visibilitychange + resize + interval` 三通道定期自检。
  - 发现节点缺失时自动重建；存在节点时强制恢复关键样式（display/visibility/position/z-index）。
  - popup 设置下发失败要有广播兜底，确保开关动作能及时到达页面。

### 用户纠偏：修复时要先消除递归链路和写入风暴，再谈可见性兜底
- 触发场景：用户反馈时间轴不可见，同时出现 `Maximum call stack size exceeded` 与 `MAX_WRITE_OPERATIONS_PER_MINUTE`。
- 本次修正规则：
  - 时间轴“找不到节点时重建”必须加重入保护，严禁 `ensure -> init -> ensure` 同步递归。
  - popup 写入 `chrome.storage.sync` 需要去抖；页面内收到设置补丁后不应再回写同一键。
  - 对关键渲染函数加轻量防重入（如导出控件渲染），优先保证稳定再扩展交互。

## 2026-03-19

### 用户纠偏：迁移外部项目时要迁能力，不要照搬对旧 DOM 的脆弱假设
- 触发场景：用户要求把 `ChatGPTHistoryCleaner` 的功能并入当前插件，但该项目依赖 `#thread` 和“2 个节点 = 1 轮”的旧版 ChatGPT 结构。
- 本次修正规则：
  - 迁移外部项目时先抽象“产品能力”和“核心策略”，再映射到当前项目已有采集链路。
  - 对 ChatGPT 轮次统计优先按 `user` 消息计轮，只有缺乏角色信息时才回退到消息数估算。
  - 不把外部项目的 popup/background/content 三段式原样并进来，优先复用当前项目的统一状态与消息装配。

### 用户纠偏：popup 去抖保存后仍要保证关闭窗口不丢最后一次设置
- 触发场景：为解决 sync 配额问题引入去抖写入后，popup 若关闭过快，最后一次改动可能未写入 storage。
- 本次修正规则：
  - popup 的延迟写入必须配套 `unload` flush 机制。
  - 动作按钮若依赖输入框当前值，不能只信任已同步 state，必须直接读取控件当前值。
  - 先保证“设置不丢”，再做去抖降频。
